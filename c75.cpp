#include<stdio.h>
int main(){
	struct student{
		int roll;
		char name[20];
		struct students;
		scanf("%d%s%d%s%d%s",&s.roll,s.name,s.addr.city,&s addr.pin);
		printf("%d%s%d%s",s.roll,s.name,s.addr.city,s.addr.pin);
		return 0;
}
